[metadata]
name = evaleval-model-cards
version = 0.1.0
description = Evidence-bound evaluation Model Cards for exact model revisions
long_description = file: README.md
long_description_content_type = text/markdown
license = MIT
license_files =
    LICENSE
    NOTICE.md

[options]
packages = find:
python_requires = >=3.9
include_package_data = False

[options.packages.find]
include = model_cards*
exclude = tests*

[options.entry_points]
console_scripts =
    model-cards = model_cards.cli:main
